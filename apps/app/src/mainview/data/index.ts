// Public API — single import point for the data package.
// Components import from `@/data` — never from internal paths.

// === Types (UI types, converters, TDLib re-export) ===
export type * as Td from 'tdlib-types';
export type {
  AlbumRenderState,
  BubbleRenderState,
  ChatState,
  HeaderStatus,
  MediaRenderState,
  MediaState,
  MessageContext,
  MessageRenderState,
  PendingRenderState,
  ServiceRenderState,
  StickerRenderState,
} from './store';
// === Store (selectors, hooks, actions, types) ===
export {
  _resetForTests,
  computeMessageState,
  selectChatMessages,
  selectContactPhotos,
  selectHeaderStatus,
  selectSearchResults,
  selectSelectedChat,
  selectUIArchivedChats,
  selectUIChats,
  useChatMessageLoader,
  useChatStore,
  useSidebarPhotoLoader,
} from './store';
export type {
  ChatKind,
  DaemonResponse,
  ErrorCode,
  MessageContentKind,
  PeerInfo,
  PendingMessage,
  SearchResultMessage,
  TelegramUpdateEvent,
  TextEntityKind,
  UIAlbumContent,
  UIAlbumItem,
  UIAnimationContent,
  UICaption,
  UIChat,
  UIChatContext,
  UIContent,
  UIDocumentContent,
  UIForward,
  UIKeyboardButton,
  UIKeyboardRow,
  UIMedia,
  UIMessage,
  UIMessageBase,
  UIPendingMessage,
  UIPhotoContent,
  UIReaction,
  UIReplyPreview,
  UIReplyTo,
  UISearchResult,
  UISender,
  UIServiceMessage,
  UIStickerContent,
  UITextContent,
  UITextEntity,
  UIUnsupportedContent,
  UIUser,
  UIVideoContent,
  UIVideoNoteContent,
  UIVoiceContent,
  UIWebPreview,
} from './types';
export {
  buildReplyPreview,
  enrichReplyPreviews,
  extractForwardName,
  extractForwardPhotoId,
  extractInlineKeyboard,
  extractMediaLabel,
  extractMessagePreview,
  extractServiceText,
  extractText,
  groupAndConvert,
  hydrateMessage,
  toChatKind,
  toUIChat,
  toUIContent,
  toUIForward,
  toUIMessage,
  toUIReactions,
  toUIReplyTo,
  toUISearchResult,
  toUISender,
  toUITextEntities,
  toUIUser,
} from './types';
